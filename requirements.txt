# Light-inspired Neural Operator — Python dependencies
# Core deep learning framework
torch>=2.0.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# HDF5 support (for loading v7.3 .mat files via h5py fallback in load_mat_file)
h5py>=3.8.0

# Visualization (used in src/eval.py for evaluation plots)
matplotlib>=3.7.0

# Colored terminal output (used in src/logger.py and src/eval.py)
termcolor>=2.3.0

# Image processing (used in make_title.py)
Pillow>=9.5.0
